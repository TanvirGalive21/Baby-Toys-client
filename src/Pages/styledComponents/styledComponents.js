import { Button } from "@mui/material";
import { styled } from "@mui/system";

const KidButton = styled(Button)({
    background: 'linear-gradient(90deg,#feeb26,coral)',
    color: '#fff',
    padding: '10px 15px'
})

export default KidButton;